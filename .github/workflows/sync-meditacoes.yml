name: Sync Meditações

# Workflow desativado temporariamente
# Para reativar, adicione o bloco "on: schedule" de volta
on:
  workflow_dispatch: # Apenas manual, sem agendamento automático

jobs:
  sync:
    if: false # Desativado — reativar quando GOOGLE_CREDENTIALS estiver configurado
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Workflow desativado"
